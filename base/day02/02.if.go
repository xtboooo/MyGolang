package main

func main() {
	/* 	a := 1
	   	b := 2
	   	if a > b {
	   		fmt.Printf("a: %v\n", a)
	   	} else {
	   		fmt.Printf("b: %v\n", b)
	   	} */

	/* 	flag1 := true
	   	if flag1 {
	   		fmt.Printf("flag: %v\n", flag1)
	   	} */

	/* 	age := 20
	   	if age > 18 {
	   		fmt.Println("你已经成年")
	   	} else {
	   		fmt.Println("你还未成年")
	   	}
	   	// age 在外部可以访问

	   	if age1 := 20; age > 18 {
	   		fmt.Printf("age1: %v\n", age1)
	   	} else {
	   		fmt.Printf("age1: %v\n", age1)
	   	}
	   	// age1 在外部无法访问 */

	// a非0无法作为bool判断
	/* 	a:=100
	   	if a{
	   	} */
}
